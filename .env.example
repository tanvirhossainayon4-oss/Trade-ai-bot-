# Frontend Variables (Public)
# URL of the deployed backend API (e.g., https://your-backend.fly.dev)
VITE_API_URL=

# URL of the deployed WebSocket server (e.g., wss://your-backend.fly.dev)
VITE_WS_URL=

# Backend Variables (Private)
# Ensure the backend server receives appropriate environment variables if any API keys are used in the future.
